/*const array1=["eye", "madam", "Ball"]
console.log(`array :`,array1)

const reversed=array1.reverse()
console.log(`reversed: `,array1)

console.log(`array1:`, array1)*/

/*let word="ball"
const wordSplitted=word.split(``)//.split splits a string into an array of substrings
const arrayReversed=wordSplitted.reverse()//in that array it makes it reversed 
const arrayJoined=arrayReversed.join(``)//arrayReversed.join makes the array into normal words
console.log(arrayJoined)*/

let word="arsenal"
const wordSplitted=word.split(``)
const arrayReverse=wordSplitted.reverse()
const arrayJoined=arrayReverse.join(``)
console.log(arrayJoined)
